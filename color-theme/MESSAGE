===========================================================================
$NetBSD$

Append the following lines to your .emacs file:

  (require 'color-theme)

Then you can choose your color-theme theme by issuing:

  M-x color-theme-select RET

===========================================================================
