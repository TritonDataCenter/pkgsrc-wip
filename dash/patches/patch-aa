$NetBSD$

--- src/mkbuiltins.orig	2007-07-13 11:26:43.000000000 +0300
+++ src/mkbuiltins
@@ -37,7 +37,7 @@
 
 tempfile=tempfile
 if ! type tempfile > /dev/null 2>&1; then
-	tempfile=mktemp
+	tempfile='mktemp /tmp/mkbuiltins.XXXXXX'
 fi
 
 trap 'rm -f $temp $temp2' EXIT
