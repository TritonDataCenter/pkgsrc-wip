===========================================================================
$NetBSD$

The class(n) man page has been moved to tclOO-class(n) as to not conflict
with tcl-itcl.

===========================================================================
