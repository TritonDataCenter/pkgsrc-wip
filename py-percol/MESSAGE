===========================================================================
$NetBSD$

This package installs a default configuration file to:
     ${PREFIX}/${PYSITELIB}/percol/rc.py

If you want to customize the configuration, copy the above file to
~/.percol.d/rc.py and edit it.

===========================================================================
