===========================================================================
$NetBSD$

Note: Google Earth *requires* DRI accelerated 3D graphics.
Note: Application tends to frequency crash on startup due to some race.
Here are possible workarounds:
- use cpuctl(8) to offline all but one CPU while starting the app
- quickly minimize the window while the application loads
===========================================================================
