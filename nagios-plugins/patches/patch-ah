$NetBSD$

--- plugins-scripts/check_ntp.pl.orig	2005-05-25 15:05:41.000000000 +0100
+++ plugins-scripts/check_ntp.pl
@@ -307,6 +307,7 @@ if ($have_ntpq) {
 				} else {
 					$jitter_error = $ERRORS{'OK'};
 				}
+				last;
 			} else {
 				print "No match!\n" if $verbose;
 				$jitter = '(not parsed)';
