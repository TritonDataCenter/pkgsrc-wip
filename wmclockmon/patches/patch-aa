$NetBSD$

--- configure.orig	Tue Feb 11 10:51:58 2003
+++ configure
@@ -4054,6 +4054,9 @@ freebsd*)
   LIBS="$LIBS -lkvm"
   SETGID_FLAGS="-g kmem -m 2755 -o root"
   ;;
+netbsd*)
+  OS=netbsd
+  ;;
 openbsd*)
   OS=openbsd
   ;;
