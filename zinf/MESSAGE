===========================================================================
$NetBSD$

Be sure to
	setenv XLIB_SKIP_ARGB_VISUALS 1
or
	export XLIB_SKIP_ARGB_VISUALS=1
before run it.

===========================================================================
