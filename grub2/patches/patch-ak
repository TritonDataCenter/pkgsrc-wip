$NetBSD$

--- util/grub.d/10_windows.in.orig	2009-11-09 16:48:16.000000000 +0100
+++ util/grub.d/10_windows.in
@@ -19,7 +19,8 @@
 prefix=@prefix@
 exec_prefix=@exec_prefix@
 libdir=@libdir@
-. ${libdir}/grub/grub-mkconfig_lib
+pkglibdirbase=${libdir}/`echo @PACKAGE_TARNAME@ | sed "@program_transform_name@"`
+. ${pkglibdirbase}/grub-mkconfig_lib
 
 case "`uname 2>/dev/null`" in
   CYGWIN*)  ;;
