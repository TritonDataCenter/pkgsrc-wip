$NetBSD$

Need to get libwrap support working ?

Need to supply a stub /etc/rc.conf.d file that will run syslog-ng
instead of the default syslogd.

Need to supply a better sample config as not everyone runs NetBSD 1.6+.
