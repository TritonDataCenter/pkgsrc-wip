===========================================================================
$NetBSD$

The vpnc-config script included in the vpnc-config package is needed to
set up DNS and routing.

To use it, invoke "openconnect" (as root) with the "-s" switch, followed 
by the path to the config script.  

More information can be found in the man page.

===========================================================================
