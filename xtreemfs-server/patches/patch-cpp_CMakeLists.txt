$NetBSD$

--- cpp/CMakeLists.txt.orig	2011-11-06 13:37:37.000000000 +0000
+++ cpp/CMakeLists.txt
@@ -40,6 +40,10 @@ if (NOT WIN32)
   SET(LIBATTR "attr")
   SET(LIBPTHREAD "pthread")
 endif(NOT WIN32)
+#IF(NETBSD)
+  SET(LIBFUSE "refuse")
+  SET(LIBATTR "")
+#ENDIF(NETBSD)
 IF(APPLE)
   # No libattr on mac.
   SET(LIBATTR "")
