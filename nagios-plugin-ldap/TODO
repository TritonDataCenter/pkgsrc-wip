# $NetBSD$
#

TODO file for nagios-plugin-ldap:
	- Makefile structure has been done to handle multiple pkg's from
the one plugins package.
	- Untested, not compiled for quite a while. It's literally wip.
