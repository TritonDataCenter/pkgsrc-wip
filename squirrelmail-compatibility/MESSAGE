===========================================================================
$NetBSD$

    This plugin needs no enabling, it's picked up automatically
    by those plugins requiring it.

===========================================================================
