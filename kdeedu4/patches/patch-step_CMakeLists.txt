$NetBSD$

Older NetBSD doesn't have fmin()

--- step/CMakeLists.txt.orig	2011-06-03 21:41:46.000000000 +0000
+++ step/CMakeLists.txt
@@ -23,6 +23,10 @@ set(STEP_WITH_QALCULATE ${QALCULATE_FOUN
 
 include_directories(${CMAKE_CURRENT_SOURCE_DIR} ${EIGEN2_INCLUDE_DIR})
 
+if(HAVE_FMIN)
+  add_definitions(-DHAVE_FMIN)
+endif(HAVE_FMIN)
+
 add_subdirectory(doc)
 add_subdirectory(stepcore)
 add_subdirectory(step)
