# $NetBSD$

BUILDLINK_DEPTH:=	${BUILDLINK_DEPTH}+
JBIGKIT_BUILDLINK3_MK:=	${JBIGKIT_BUILDLINK3_MK}+

.if ${BUILDLINK_DEPTH} == "+"
BUILDLINK_DEPENDS+=	jbigkit
.endif

BUILDLINK_PACKAGES:=	${BUILDLINK_PACKAGES:Njbigkit}
BUILDLINK_PACKAGES+=	jbigkit
BUILDLINK_ORDER:=	${BUILDLINK_ORDER} ${BUILDLINK_DEPTH}jbigkit

.if ${JBIGKIT_BUILDLINK3_MK} == "+"
BUILDLINK_API_DEPENDS.jbigkit+=	jbigkit>=1.6
BUILDLINK_PKGSRCDIR.jbigkit?=	../../wip/jbigkit
.endif	# JBIGKIT_BUILDLINK3_MK

BUILDLINK_DEPTH:=	${BUILDLINK_DEPTH:S/+$//}
