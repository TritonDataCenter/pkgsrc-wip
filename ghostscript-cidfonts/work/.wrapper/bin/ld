#! /bin/sh
empty_file=""
arg_pp="/export/pkgsrc/wip/ghostscript-cidfonts/work/.wrapper/tmp/empty"
arg_pp_main="/export/pkgsrc/wip/ghostscript-cidfonts/work/.wrapper/tmp/arg-pp-main"
arg_source="/export/pkgsrc/wip/ghostscript-cidfonts/work/.wrapper/tmp/arg-source"
buildcmd="/export/pkgsrc/wip/ghostscript-cidfonts/work/.wrapper/tmp/buildcmd"
cache="/export/pkgsrc/wip/ghostscript-cidfonts/work/.wrapper/tmp/cache"
cache_body="/export/pkgsrc/wip/ghostscript-cidfonts/work/.wrapper/tmp/cache-body"
cleanup="/export/pkgsrc/wip/ghostscript-cidfonts/work/.wrapper/tmp/empty"
cmd_sink="/export/pkgsrc/wip/ghostscript-cidfonts/work/.wrapper/tmp/cmd-sink-ld"
logic="/export/pkgsrc/wip/ghostscript-cidfonts/work/.wrapper/tmp/logic"
reorderlibs="/export/pkgsrc/wip/ghostscript-cidfonts/work/.wrapper/tmp/reorderlibs"
shell_lib="/export/pkgsrc/wip/ghostscript-cidfonts/work/.wrapper/tmp/shell-lib"
scan="/export/pkgsrc/wip/ghostscript-cidfonts/work/.wrapper/tmp/scan"
transform="/export/pkgsrc/wip/ghostscript-cidfonts/work/.wrapper/tmp/empty"
transform_sed=" -f /export/pkgsrc/wip/ghostscript-cidfonts/work/.wrapper/tmp/transform.sed"
wrapper_subr_sh="/export/pkgsrc/wip/ghostscript-cidfonts/work/.wrapper/tmp/wrapper-subr.sh"
wrapper_abi=""		# see ABI in mk/defaults/mk.conf
wrapper_type="LD"	# one of AS, CC, CXX, FC, IMAKE, LD, LIBTOOL.
wrapperlog="${WRAPPER_LOG-/export/pkgsrc/wip/ghostscript-cidfonts/work/.work.log}"
skip_transform="${WRAPPER_SKIP_TRANSFORM-no}"
debug="${WRAPPER_DEBUG-no}"
cat="/bin/cat"
echo="echo"
expr="/bin/expr"
mv="/bin/mv"
sed="/usr/bin/sed"
test="test"
Xsed="$sed -e 1s/^X//"
. $shell_lib
. "$wrapper_subr_sh"
updatecache=${WRAPPER_UPDATE_CACHE-yes}
reorder=${WRAPPER_REORDER-no}
case $debug in
yes)	debug_log="msg_log" ;;
*)	debug_log=":" ;;
esac
qcmd="$0"; for arg in ${1+"$@"}; do shquote "$arg"; qcmd="$qcmd $shquoted"; done
msg_log "$wrapperlog" "[*] $0 $qcmd"
cmd="/usr/bin/ld"
case "$*" in
--wrappee-name) $echo $cmd ; exit ;;
esac
libs=""
rellpath=""
do_transform=yes
init_queue argbuf
init_queue cmdbuf
append_extra_args=yes
. $scan
case $append_extra_args in
yes)
	$debug_log $wrapperlog "    (wrapper.sh) append args: -L/export/pkgsrc/wip/ghostscript-cidfonts/work/.buildlink/lib"
	set -- "$@" -L/export/pkgsrc/wip/ghostscript-cidfonts/work/.buildlink/lib
	;;
*)
	;;
esac
. $arg_source
. $logic
. $cmd_sink
case $reorder in
yes)
	if $test -n "$libs"; then
		. $reorderlibs
	fi
	;;
esac
cmd="$cmd $libs"
PATH="/export/pkgsrc/wip/ghostscript-cidfonts/work/.buildlink/bin:/export/pkgsrc/wip/ghostscript-cidfonts/work/.tools/bin:/usr/pkg/bin:/usr/pkg/bin:/usr/local/bin:/usr/bin:/bin:/usr/pkg/sbin:/usr/local/sbin:/usr/sbin:/sbin"; export PATH
msg_log $wrapperlog "<.>" "$cmd"
if [ "${empty_file}" = "${cleanup}" ] && [ "$debug" != "yes" ]; then
	eval "exec $cmd"
	wrapper_results=$?
else
	wrapper_result=0
	eval "$cmd" || wrapper_result="$?"
fi
. $cleanup
[ $wrapper_result -eq 0 ] || [ "$debug" != "yes" ] || {
	echo ""
	echo "[wrapper.sh] note: The real command line, after the pkgsrc wrapper, was:"
	echo "$cmd"
} 1>&2
exit ${wrapper_result}
