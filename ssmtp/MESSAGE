===========================================================================
$NetBSD$

sSMTP has been installed successfully.

*BEFORE* using the program, edit ${PKG_SYSCONFDIR}/ssmtp.conf and
${PKG_SYSCONFDIR}/revaliases -- especially, the variables `mailhub'
and `hostname' in ssmtp.conf.

===========================================================================
