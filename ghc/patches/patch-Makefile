$NetBSD$

Required by patches/patch-ghc.mk


--- Makefile.orig	2013-04-18 21:22:46.000000000 +0000
+++ Makefile
@@ -75,7 +75,7 @@ binary-dist: binary-dist-prep
 ifeq "$(mingw32_TARGET_OS)" "1"
 	mv bindistprep/*.exe .
 endif
-	mv bindistprep/*.tar.bz2 .
+	mv bindistprep/*.tar .
 
 binary-dist-prep:
 ifeq "$(mingw32_TARGET_OS)" "1"
