$NetBSD$

--- src/libs/zbxsysinfo/netbsd/inodes.c.orig	2007-12-07 02:23:38.000000000 +0300
+++ src/libs/zbxsysinfo/netbsd/inodes.c	2007-12-07 02:23:52.000000000 +0300
@@ -25,11 +25,11 @@
 
 static int	get_fs_inodes_stat(char *fs, double *total, double *free, double *usage)
 {
-	struct statfs   s;
+	struct statvfs   s;
 	
 	assert(fs);
 	
-	if ( statfs( fs, &s) != 0 ) 
+	if ( statvfs( fs, &s) != 0 ) 
 	{
 		return	SYSINFO_RET_FAIL;
 	}
