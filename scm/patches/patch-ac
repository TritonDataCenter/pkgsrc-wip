$NetBSD$

--- crs.c.orig	Sat Apr 13 13:35:53 2002
+++ crs.c
@@ -44,7 +44,7 @@
 
 #include "scm.h"
 
-#ifdef __FreeBSD__
+#if defined(__FreeBSD__) || defined(__NetBSD__)
 # include <ncurses.h>
 #else
 # include <curses.h>
