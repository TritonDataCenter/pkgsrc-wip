$NetBSD$

--- src/bgl_babylon.cpp.orig	2007-08-16 08:14:35.000000000 +0300
+++ src/bgl_babylon.cpp
@@ -390,7 +390,7 @@ void Babylon::convertToUtf8( std::string
   const char *inbuf;
   inbuf = s.data();
 #else
-  char *inbuf;
+  const char *inbuf;
   inbuf = (char *)s.data();
 #endif
   outbuf = (char*)malloc( outbufbytes + 1 );
