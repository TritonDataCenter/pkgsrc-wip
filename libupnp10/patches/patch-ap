$NetBSD$

--- src/genlib/util/Makefile.orig	2001-06-15 09:22:16.000000000 +0900
+++ src/genlib/util/Makefile
@@ -44,7 +44,7 @@ objects = genexception.o xstring.o memre
 
 all: $(objects) $(lib_dir)/utilall.o
 
-CFLAGS = -I $(inc_root) -fpic -Wall -c -D_REENTRANT
+CFLAGS = -I $(inc_root) -fpic -Wall -c $(PTHREAD_CFLAGS)
 
 ifeq ($(WEB),1)
 CFLAGS += -DINTERNAL_WEB_SERVER
