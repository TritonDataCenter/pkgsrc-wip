$NetBSD$

--- haskore.cabal.orig	2009-01-26 08:38:34.000000000 +0900
+++ haskore.cabal
@@ -51,7 +51,7 @@ Library
     transformers >=0.0.1 && <0.2,
     bytestring >=0.9 && <0.10,
     haskell-src >=1.0 && <1.1,
-    parsec >=2.1 && <2.2
+    parsec >=2.1
 
   If flag(splitBase)
     Build-Depends:
