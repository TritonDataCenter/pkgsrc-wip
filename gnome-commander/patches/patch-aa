$NetBSD$

--- configure.orig	2006-05-07 16:21:30.000000000 +0200
+++ configure
@@ -21722,7 +21722,7 @@ _ACEOF
 
 
 
-PLUGIN_DIR="${libdir}/${PACKAGE}/plugins"
+PLUGIN_DIR="${prefix}/lib/${PACKAGE}/plugins"
 
 cat >>confdefs.h <<_ACEOF
 #define PLUGIN_DIR "$PLUGIN_DIR"
