$NetBSD$

--- client/Thread.cpp.orig	2006-05-26 23:06:16.000000000 +0200
+++ client/Thread.cpp
@@ -40,4 +40,8 @@ void Thread::start() throw(ThreadExcepti
 }
 #endif
+
+#ifndef _WIN32
+pthread_mutex_t Thread::safeinc_mutex = PTHREAD_MUTEX_INITIALIZER;
+#endif
 /**
  * @file
