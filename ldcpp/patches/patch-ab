$NetBSD$

--- linux/prefix.cc.orig	2006-02-19 02:09:01.000000000 +0100
+++ linux/prefix.cc
@@ -46,8 +46,10 @@ extern "C" {
 #endif /* __cplusplus */
 
 
+/*
 #undef NULL
 #define NULL ((void *) 0)
+*/
 
 #ifdef __GNUC__
 	#define br_return_val_if_fail(expr,val) if (!(expr)) {fprintf (stderr, "** BinReloc (%s): assertion %s failed\n", __PRETTY_FUNCTION__, #expr); return val;}
