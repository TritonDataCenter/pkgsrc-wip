$NetBSD$

--- Makefile.rules.orig	2010-07-14 10:19:32.000000000 +0400
+++ Makefile.rules	2010-07-14 10:22:46.000000000 +0400
@@ -626,9 +626,9 @@
 ifdef TOOLNAME
   LD.Flags += $(RPATH) -Wl,'$$ORIGIN/../lib'
   ifdef EXAMPLE_TOOL
-    LD.Flags += $(RPATH) -Wl,$(ExmplDir) $(RDYNAMIC)
+    LD.Flags += $(RDYNAMIC)
   else
-    LD.Flags += $(RPATH) -Wl,$(ToolDir) $(RDYNAMIC)
+    LD.Flags += $(RDYNAMIC)
   endif
 endif
 endif
