$NetBSD$

--- utils/SJISconv/Makefile.orig	Thu Jan  1 08:00:00 1970
+++ utils/SJISconv/Makefile	Sat Feb 20 07:29:10 1999
@@ -0,0 +1,14 @@
+sjisconv:
+	$(CC) $(CFLAGS) -o $@ sjisconv.c
+
+build: sjisconv
+
+all: build
+
+install: all
+	$(BSD_INSTALL_PROGRAM) sjisconv $(PREFIX)/bin
+	$(BSD_INSTALL_SCRIPT) sjislatex $(PREFIX)/bin
+	$(BSD_INSTALL_MAN) sjisconv.1 $(PREFIX)/man/man1
+
+clean:
+	rm -f sjisconv
