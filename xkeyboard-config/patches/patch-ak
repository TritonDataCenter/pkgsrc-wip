$NetBSD$

--- rules/merge.sh.orig	2007-11-08 22:29:35.000000000 -0500
+++ rules/merge.sh
@@ -10,7 +10,7 @@ fi
 > $DEST
 
 for i in $*; do
-  if [ "$i" == "$HDR" ] || [ "$i" == "HDR" ]; then
+  if [ "$i" = "$HDR" ] || [ "$i" = "HDR" ]; then
     echo >> $DEST;
     read hdr
     echo "$hdr" >> $DEST
