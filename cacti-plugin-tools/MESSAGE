===========================================================================
$NetBSD$

To activate the plugin, add this to ${CACTIDIR}/include/global.php:

        $plugins[] = 'tools';

===========================================================================
