# $NetBSD$
#

.if !defined(MINGW_MAKEFILECOMMON)
MINGW_MAKEFILECOMMON=	# defined

MASTER_SITE_MINGW=    	${MASTER_SITE_SOURCEFORGE:=mingw/}
MINGW_TARGET?=		i586-pc-mingw32
PLIST_SUBST+=		MINGW_TARGET=${MINGW_TARGET}
.endif
