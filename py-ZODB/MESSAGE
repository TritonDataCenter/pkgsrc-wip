===========================================================================
$NetBSD$

To start the server, bound to port 9999:

	${PREFIX}/bin/runzeo.py -f DBNAME -a 9999

===========================================================================
