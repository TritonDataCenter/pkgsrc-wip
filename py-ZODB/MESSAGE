===========================================================================
$NetBSD$

To start-up, run:
   ${PREFIX}/bin/mkzeoinst.py
   
then:
  ${PREFIX}/bin/runzeo.py

===========================================================================
