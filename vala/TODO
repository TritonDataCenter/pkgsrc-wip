$NetBSD$

Remaining tasks:
	- split up documentation and/or vapi generator, etc. into
	  seperate packages.
	- Where should the devhelp files go?  By default, they're
	  installed into share/devhelp, but that directory is
	  part of devel/devhelp ...
	- See if it works.
