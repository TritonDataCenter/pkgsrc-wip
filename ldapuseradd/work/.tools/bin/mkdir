#!/bin/sh
wrapperlog="${TOOLS_WRAPPER_LOG-/usr/pkgsrc/wip/ldapuseradd/work/.work.log}"
echo "[*] "/usr/pkgsrc/wip/ldapuseradd/work/.tools/bin/mkdir" $@" >> $wrapperlog
echo "<.> /bin/mkdir -p "$@"" >> $wrapperlog
/bin/mkdir -p "$@"
