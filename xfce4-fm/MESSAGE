===========================================================================
$NetBSD$

You will need to install the samba package (pkgsrc/net/samba)
before attempting to browse any Windows shares in your network
with ${PKGNAME}.
===========================================================================
