===========================================================================
$NetBSD$

In ~/.profile add:
	export JAZZ=${PREFIX}/share/jazz

===========================================================================
