$NetBSD$

--- src/config.h.orig	Fri Mar  3 11:03:57 2000
+++ src/config.h
@@ -41,7 +41,7 @@
 // support jazz's own driver over tcp/ip
 #define DEV_MPU401
 
-#define DEV_ALSA
+#undef DEV_ALSA
 
 // in ms-windows, we have nothing
 #ifdef wx_msw
