$NetBSD$

--- src/util.c.orig	Mon Nov 25 21:02:50 2002
+++ src/util.c
@@ -40,7 +40,7 @@
  */
 #define OPEN_TTY() getpt()
 
-#ifdef __FreeBSD__
+#if defined(__FreeBSD__) || defined(__NetBSD__)
 /* FreeBSD doesn't have getpt(). This function emulates it's behaviour. */
 int getpt (void);
 
