===========================================================================
$NetBSD$

To compile against the Florist packages,
add:

 ${PREFIX}/lib/florist

to your ADA_INCLUDE_PATH.

===========================================================================
