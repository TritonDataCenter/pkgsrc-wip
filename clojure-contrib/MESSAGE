===========================================================================
$NetBSD$

To enable this package, you should make .clojure file
in your working directory.
And your .closure should be the following.

$ cat .clojure
${PREFIX}/lib/java/clojure/clojure-contrib.jar
===========================================================================
