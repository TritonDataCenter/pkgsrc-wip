===========================================================================
$NetBSD$

To enable wip/clojure-contrib, you should make .clojure file
in your working directory.

For example, .clojure should be the following.

$ cat .clojure
${PREFIX}/lib/java/clojure/clojure-contrib.jar
===========================================================================
