===========================================================================
$NetBSD$

Add the following line to httpd.conf to make TWiki accessible
through your HTTP server:

	Include ${PKG_SYSCONFDIR}/mediawiki.conf

Your MediaWiki is the accessible through:

	http://www.example.com/mediawiki/

===========================================================================
