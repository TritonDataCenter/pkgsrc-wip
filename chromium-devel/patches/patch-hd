$NetBSD$

--- net/socket/ssl_client_socket.h.orig	2010-08-29 09:04:25.000000000 +0000
+++ net/socket/ssl_client_socket.h
@@ -30,6 +30,9 @@ class DNSSECProvider {
   //      function will return OK if called again.
   virtual int GetDNSSECRecords(RRResponse** out,
                                CompletionCallback* callback) = 0;
+
+ private:
+  virtual ~DNSSECProvider();
 };
 
 // A client socket that uses SSL as the transport layer.
