$NetBSD$

--- third_party/WebKit/WebCore/platform/graphics/chromium/FontPlatformData.h.orig	2010-01-06 10:06:18.000000000 +0000
+++ third_party/WebKit/WebCore/platform/graphics/chromium/FontPlatformData.h
@@ -33,7 +33,7 @@
 
 #if OS(WINDOWS)
 #include "FontPlatformDataChromiumWin.h"
-#elif OS(LINUX)
+#elif OS(LINUX) || OS(FREEBSD) || OS(NETBSD)
 #include "FontPlatformDataLinux.h"
 #endif
 
