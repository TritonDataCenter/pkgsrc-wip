$NetBSD$

--- iris/iris.pri.orig	2007-10-14 09:00:49.000000000 -0500
+++ iris/iris.pri
@@ -1,7 +1,10 @@
 # libidn
-LIBIDN_BASE = $$PWD/libidn
-CONFIG += libidn
-include($$PWD/libidn.pri)
+# LIBIDN_BASE = $$PWD/libidn
+# CONFIG += libidn
+# include($$PWD/libidn.pri)
+unix:{
+	LIBS += -lidn
+}
 
 # IrisNet
 irisnet {
