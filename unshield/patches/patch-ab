$NetBSD$

--- lib/libunshield.c.orig	2004-08-24 07:10:27.000000000 +0100
+++ lib/libunshield.c
@@ -27,7 +27,7 @@ static bool unshield_create_filename_pat
 
     for (; *p != '\0'; p++)
     {
-      if ('.' == *p || isdigit(*p))
+      if ('.' == *p || isdigit((int)*p))
       {
         *p = '\0';
         break;
