$NetBSD$

--- configure.in.orig	2008-09-25 02:20:51.000000000 +0000
+++ configure.in
@@ -32,7 +32,6 @@ AC_SUBST(version_info)
 LT_PROG_RC
 AC_FUNC_ALLOCA
 
-MODULE_ARCH="$host_os-$host_cpu"
 AC_SUBST(MODULE_ARCH)
 AC_DEFINE_UNQUOTED(MODULE_ARCH, "$MODULE_ARCH", "Module architecture")
 
