===========================================================================
$NetBSD$

You must edit your "${PKG_SYSCONFDIR}/etc/php.ini" if you want
to use PEAR packages:

  register_argc_argv = On
  register_globals = On

===========================================================================
