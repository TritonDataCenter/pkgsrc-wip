$NetBSD$

--- configure.in.orig	2006-09-14 13:05:02.000000000 +0000
+++ configure.in
@@ -231,7 +231,7 @@ AC_CHECK_LIB(compat,ftime)
 AC_MSG_CHECKING([which threading environment to use])
 # each host OS needs special threading flags
 case $build_os in
-	freebsd*)
+	freebsd*|netbsd*)
 		LIBS="-pthread $LIBS"
 		AC_MSG_RESULT([-pthread])
 		;;
