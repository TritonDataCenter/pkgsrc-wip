$NetBSD$

--- eval.c.orig	2004-07-06 16:19:42.000000000 +0300
+++ eval.c
@@ -64,6 +64,9 @@ __RCSID("$NetBSD: eval.c,v 1.19 2004/07/
 #include "extern.h"
 #include "pathnames.h"
 
+size_t
+     strlcpy(char *dst, const char *src, size_t size);
+
 #define BUILTIN_MARKER	"__builtin_"
 
 static void	dodefn __P((const char *));
