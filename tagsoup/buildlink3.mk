# $NetBSD$

BUILDLINK_DEPTH:=	${BUILDLINK_DEPTH}+
TAGSOUP_BUILDLINK3_MK:=	${TAGSOUP_BUILDLINK3_MK}+

.if ${BUILDLINK_DEPTH} == "+"
BUILDLINK_DEPENDS+=	tagsoup
.endif

BUILDLINK_PACKAGES:=	${BUILDLINK_PACKAGES:Ntagsoup}
BUILDLINK_PACKAGES+=	tagsoup
BUILDLINK_ORDER:=	${BUILDLINK_ORDER} ${BUILDLINK_DEPTH}tagsoup

.if ${TAGSOUP_BUILDLINK3_MK} == "+"
BUILDLINK_DEPMETHOD.tagsoup?=	build
BUILDLINK_API_DEPENDS.tagsoup+=	tagsoup>=0.6
BUILDLINK_PKGSRCDIR.tagsoup?=	../../wip/tagsoup
.endif	# TAGSOUP_BUILDLINK3_MK

BUILDLINK_DEPTH:=	${BUILDLINK_DEPTH:S/+$//}
