$NetBSD$

--- ./jdk/make/java/npt/Makefile.orig	2008-12-21 21:41:15.000000000 +0100
+++ ./jdk/make/java/npt/Makefile
@@ -67,9 +67,11 @@ endif
 
 # Add location of iconv headers
 ifeq ($(PLATFORM), bsd)
+ ifneq ($(SYSTEM_UNAME),NetBSD)
   ICONV_PATH = $(PKG_PATH)
   CPPFLAGS += -I$(ICONV_PATH)/include
   OTHER_LDLIBS += -L$(ICONV_PATH)/lib -liconv
+ endif
 endif
 
 #
