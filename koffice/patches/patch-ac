$NetBSD$

--- kspread/functions/financial.cpp.orig	2010-12-29 10:36:00.000000000 +0000
+++ kspread/functions/financial.cpp
@@ -41,6 +41,10 @@
 
 #include <math.h>
 
+#ifdef __NetBSD__
+#define fmin(a,b) ((a) < (b) ? (a) : (b))
+#endif
+
 using namespace KSpread;
 
 // prototypes (sorted)
