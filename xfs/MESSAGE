===========================================================================
$NetBSD$

For configuration file,
  cp -p ${PREFIX}/etc/X11/fs/config  /etc/X11/fs

===========================================================================
