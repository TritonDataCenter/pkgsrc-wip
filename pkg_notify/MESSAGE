===========================================================================
$NetBSD$

Create the ${LOCALBASE}/etc/pkg_notify.list file containing the package
list you want to be informed on, following this format :

$ cat ${LOCALBASE}/etc/pkg_notify.list
wip/foo
net/bar
www/foobar-devel

===========================================================================
