$NetBSD$

--- Makefile.inc.orig	Tue Apr  8 09:36:00 2003
+++ Makefile.inc
@@ -1,9 +1,8 @@
 CC := gcc
-CFLAGS := -O2 -ffast-math -fomit-frame-pointer
 CFLAGS += `glib-config --cflags` `gtk-config --cflags`
 
 $(PLUGIN_FILE): $(OBJECTS)
-	$(CC) -o $@ $^ -ldumb -lxmms -shared
+	$(CC) -o $@ $^ -ldumb -lxmms -shared `pkg-config gtk+ --libs`
 
 $(OBJDIR)/%.o: %.c
 	$(CC) -c -o $@ $(CFLAGS) $<
