$NetBSD$

--- dkim-filter/dkim-db.c.orig	2008-03-11 22:17:25.000000000 +0100
+++ dkim-filter/dkim-db.c	2008-03-30 12:45:43.000000000 +0200
@@ -60,8 +60,10 @@ dkimf_db_open(DB **db, const char *file,
 	else
 	{
 		flags |= DB_CREATE;
-		dbtype = DB_BTREE;
+		dbtype = DB_HASH;
 	}
+#else /* DB_VERSION_CHECK(2,0,0) */
+	dbtype = DB_HASH;
 #endif /* DB_VERSION_CHECK(2,0,0) */
 
 #if DB_VERSION_CHECK(3,0,0)
