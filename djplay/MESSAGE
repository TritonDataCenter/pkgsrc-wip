===========================================================================
$NetBSD$

DJPlay needs jackd running as the same user as the user executing DJPlay in
order to start. jackd also needs to be using the oss backend, as DJPlay
didn't seem to work with the sun backend.

DJPlay crashes on the first run, but executes on the subsequent instance.

===========================================================================
