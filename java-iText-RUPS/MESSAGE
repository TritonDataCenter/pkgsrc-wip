===========================================================================
$NetBSD$

	Add:
		${PREFIX}/lib/java/${DN}.jar
	to your CLASSPATH environ.

===========================================================================
