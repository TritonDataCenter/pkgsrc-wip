$NetBSD$

--- ntpd.h.orig	Wed Mar 23 11:42:04 2005
+++ ntpd.h	Sun Apr 10 23:47:17 2005
@@ -30,7 +30,13 @@
 
 #include "ntp.h"
 
-#define	NTPD_USER	"_ntp"
+#ifndef SA_LEN
+#  define SA_LEN(s) ((s)->sa_len)
+#endif
+#ifdef __NetBSD__
+#  define arc4random random
+#endif
+
 #define	CONFFILE	"/etc/ntpd.conf"
 
 #define	READ_BUF_SIZE		65535
