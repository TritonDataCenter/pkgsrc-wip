===========================================================================
$NetBSD$

Plugins can be auto-configured with "munin-node-configure --shell | sh"

===========================================================================
