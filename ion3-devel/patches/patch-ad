$NetBSD$

--- pwm/Makefile.orig	2008-01-03 16:24:17.000000000 -0200
+++ pwm/Makefile
@@ -73,5 +73,5 @@ _install:
 	$(INSTALLBIN) pwm3 $(BINDIR)
 	$(INSTALLDIR) $(PWM_ETCDIR)
 	for i in $(ETC); do \
-		$(INSTALL) -m $(DATA_MODE) $$i $(PWM_ETCDIR); \
+		$(INSTALL) -m $(DATA_MODE) $$i $(EXAMPLEDIR); \
 	done
