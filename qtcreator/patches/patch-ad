$NetBSD$

--- src/app/app.pro.orig	2009-07-10 12:38:08 +0000
+++ src/app/app.pro
@@ -23,6 +23,6 @@ win32 {
 } else {
     LIBS *= -lExtensionSystem -lAggregation
 
-    target.path  = /bin
+    target.path  = /$(PREFIX)/bin
     INSTALLS    += target
 }
