===========================================================================
$NetBSD$

You should read ${EGDIR}/README.TLS to learn how to create
and install your certificate.

${PKGBASE} can be ran either in daemon mode with the ${RCD_SCRIPTS} script
or via the super-internet-daemon inetd.

===========================================================================
