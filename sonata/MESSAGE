===========================================================================
$NetBSD$
If you encounter a silly error message like "Unlocking unlocked mutex.", this
is probably a bug in the Python pthreads implementation. Setting the environment
variable PTHREAD_DIAGASSERT to "A" (ignore errors) makes it work fine for the
time being.
===========================================================================
