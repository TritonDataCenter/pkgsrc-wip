$NetBSD$

--- Makefile.orig	2008-08-05 20:55:54.000000000 +0000
+++ Makefile
@@ -16,7 +16,7 @@ OS_ARCH	= $(shell uname)
 ifeq ($(OS_ARCH), SunOS)
 LDFLAGS = -lpci -lz
 else
-LDFLAGS = -lpci -lz
+LDFLAGS = -lpci ${LDLIBS} -static
 STRIP_ARGS = -s
 endif
 ifeq ($(OS_ARCH), FreeBSD)
